[core]
	editor = code --wait
[alias]
	s = status
	l = log --oneline
	cm = commit -m
	a = add
	p = push
	ds = diff --staged
[push]
	autoSetupRemote = true
[http]
	postBuffer = 524288000

# ローカル設定（名前、メールアドレスなど）を読み込む
# ~/.gitconfig.local に個人情報を記載してください
[include]
	path = ~/.gitconfig.local
